/**
 * support.jsx  (tab screen)
 *
 * Renders the SupportTicketList directly as the Support tab.
 * The list screen handles its own header and navigation.
 */

import SupportTicketList from '../screens/support/supportTicketList';

export default SupportTicketList;
